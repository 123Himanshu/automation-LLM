# ExcelFlow API - Environment Variables

# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host.neon.tech/excelflow?sslmode=require

# Authentication
BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=changeme

# S3 Storage (AWS S3, MinIO, or any S3-compatible service)
S3_BUCKET=excelflow-storage
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key
# S3_ENDPOINT=http://localhost:9000  # Uncomment for MinIO / LocalStack

MAX_UPLOAD_SIZE_MB=50

# AI — Dual Provider Setup
# Both providers can be configured simultaneously.
# OpenAI is used for Excel/spreadsheet AI features (structured JSON, tool calls).
# Groq is used for LLM chat, PDF workspace, DOCX workspace (fast inference).

# OpenAI (for Excel features)
AI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o

# Groq (for LLM chat, PDF, DOCX)
GROQ_API_KEY=gsk_your_groq_api_key
AI_MODEL=llama-3.3-70b-versatile

# Optional custom OpenAI-compatible base URL (applies to OpenAI slot only).
# AI_BASE_URL=

AI_MAX_TOKENS=4096

# Server
PORT=4000
NODE_ENV=development

# CORS - comma-separated origins for production
# Example: https://excelflow.vercel.app,http://localhost:3000
FRONTEND_URL=http://localhost:3000
